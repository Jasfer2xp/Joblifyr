# Django API base URL (no trailing slash)
# Local dev with Vite proxy: leave empty
# Production (Vercel): https://your-django-backend.example.com
VITE_API_URL=

# Optional: Vite dev proxy target (defaults to http://localhost:8000)
# VITE_DEV_PROXY=http://localhost:8000
